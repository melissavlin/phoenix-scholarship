<br/>

<h2> <%= "#{current_user.nickname}'s Profile" %> </h2>
<br/>
<ul>
		<li><%= "#{current_user.fname} #{current_user.lname}" %></li>
		<li><%= "#{current_user.nickname}" %></li>
		<li><%= "Status : #{current_user.status}" %>

			<% if can? :read, @all_apps %>
			<%= " and Boardmember" %>
			<% end %>

		</li>
		<li><%= "Upsilon #{current_user.greek_class}" %></li>
		<li><%= "Graduation Year : #{current_user.gradyr}" %></li>
</ul>

<br/>
<ul>
	<li> <%= link_to "edit info", edit_user_registration_path %> </li>

	<li> <%= link_to "all users", users_roster_path %> </li>

	<% if can? :manage, @donate %>	
		<li> <%= link_to "your contributions", new_user_donation_path(current_user) %> </li>
	<% end %>

	<% if can? :create, @app %>
		<li> <%= link_to "apply", new_user_app_path(current_user) %> </li>
	<% end %>


		<li> <%= link_to "my applications", user_apps_path(current_user) %> </li>


	<% if can? :read, @all_apps %>
		<li> <%= link_to "read applications", apps_review_path %> </li>
	<% end %>

</ul>


